{
  "info": {
    "_postman_id": "manager-assignment-collection",
    "name": "LPG Tamale - Station Manager Assignment",
    "description": "Complete API collection for managing station manager assignments with CRUD operations and historical tracking",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "1. Assign Manager to Station",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{token}}",
            "type": "text"
          },
          {
            "key": "Content-Type",
            "value": "application/json",
            "type": "text"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"manager_id\": {{manager_id}},\n  \"removal_reason\": null\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/stations/{{station_id}}/assign-manager",
          "protocol": "http",
          "host": ["{{base_url}}"],
          "path": ["api", "stations", "{{station_id}}", "assign-manager"]
        },
        "description": "Assign a manager (with 'station' role) to a station.\n\n**Authorization:** Admin only\n**Validation:**\n- Manager must have 'station' role\n- Manager must be active\n- Existing active manager will be automatically removed\n\n**Response:** 201 Created with assignment details"
      }
    },
    {
      "name": "2. Get Current Manager",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{token}}",
            "type": "text"
          }
        ],
        "url": {
          "raw": "{{base_url}}/api/stations/{{station_id}}/manager",
          "protocol": "http",
          "host": ["{{base_url}}"],
          "path": ["api", "stations", "{{station_id}}", "manager"]
        },
        "description": "Retrieve the currently active manager assigned to a station.\n\n**Authorization:** Admin only\n**Returns:** Current assignment with manager details or null if no active manager"
      }
    },
    {
      "name": "3. Get Manager Assignment History",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{token}}",
            "type": "text"
          }
        ],
        "url": {
          "raw": "{{base_url}}/api/stations/{{station_id}}/manager-history?per_page=15",
          "protocol": "http",
          "host": ["{{base_url}}"],
          "path": ["api", "stations", "{{station_id}}", "manager-history"],
          "query": [
            {
              "key": "per_page",
              "value": "15"
            },
            {
              "key": "manager_id",
              "value": "",
              "disabled": true
            }
          ]
        },
        "description": "Get the complete assignment history for a station, showing all current and past managers.\n\n**Authorization:** Admin only\n**Query Parameters:**\n- `per_page` (optional): Number of records per page (default: 15)\n- `manager_id` (optional): Filter by specific manager ID\n\n**Returns:** Paginated list with full details including removal reasons"
      }
    },
    {
      "name": "4. Remove Manager from Station",
      "request": {
        "method": "DELETE",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{token}}",
            "type": "text"
          },
          {
            "key": "Content-Type",
            "value": "application/json",
            "type": "text"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"removal_reason\": \"Manager transferred to another station\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/stations/{{station_id}}/remove-manager",
          "protocol": "http",
          "host": ["{{base_url}}"],
          "path": ["api", "stations", "{{station_id}}", "remove-manager"]
        },
        "description": "Remove the currently active manager from a station.\n\n**Authorization:** Admin only\n**Behavior:**\n- Sets `removed_at` timestamp to current time\n- Tracks removal reason for audit trail\n- Does NOT delete historical record\n\n**Response:** 200 OK with updated assignment record"
      }
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:8001",
      "type": "string"
    },
    {
      "key": "token",
      "value": "",
      "type": "string",
      "description": "Admin bearer token from login endpoint"
    },
    {
      "key": "station_id",
      "value": "019b396b-dd20-7120-bedb-858432d894c3",
      "type": "string",
      "description": "UUID of the station"
    },
    {
      "key": "manager_id",
      "value": "3",
      "type": "string",
      "description": "ID of the user with 'station' role to assign as manager"
    }
  ]
}
